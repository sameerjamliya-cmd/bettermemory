OPENAI_API_KEY=
QDRANT_URL=http://localhost:6333

# Optional. Defaults to gpt-4o-mini when unset.
# Set to gpt-4o to use the stronger extraction model (measured: better on
# multi-dimension supersession and paraphrase dedup, ~10x per-token cost).
# EXTRACTION_MODEL=
